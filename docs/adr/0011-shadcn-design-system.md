# shadcn/ui design system

UI is **shadcn/ui**, preset `b1VlJj2R` (luma / neutral / phosphor / inter) on Tailwind v4 — Base UI, not Radix. One token added: `--success`, because neutral only has `destructive` and a deploy screen must say "it is running" without reading as an error. Prefer `bunx shadcn add` over hand-rolled primitives.

**Status:** accepted
