# Public repository

The GitHub repo is **PUBLIC**. AGPL-3.0 already implied openness, and public
visibility makes HTTPS `git fetch` usable as an update transport with no token on
target machines. Decided 2026-08-07; actually flipped 2026-08-08 after `gh repo
view` still reported PRIVATE.

**Status:** accepted
