# Build on the target server

Images are built **on the target server**, not on a separate builder fleet.
"One command on any VPS" is a core requirement; building elsewhere needs an
always-on machine or a registry hop before the first deploy.

**Status:** accepted
