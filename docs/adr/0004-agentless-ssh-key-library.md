# Agentless SSH access from a key library

Targets are reached **agentless, over SSH only** — nothing to install on the
server. The key comes from the **`ssh_keys` library** (reversal done and verified
2026-08-07). Do not reintroduce per-target ad-hoc keys as the primary model.

**Status:** accepted
