/**
 * The product's one-line description.
 *
 * One constant because it is rendered twice — as the page `description` meta
 * and on the sign-in screen — and the two had drifted into different
 * wordings of the same sentence.
 */
export const TAGLINE = "Deploy from a git repo to a server you own.";
