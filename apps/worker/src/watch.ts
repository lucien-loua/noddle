/**
 * biome-ignore-all lint/performance/noBarrelFile: worker `#watch` alias kept for verifies
 */
export {
  CRASH_LOOP_THRESHOLD,
  inspectServiceHealth,
  WATCH_WINDOW_MS,
  type WatchVerdict,
  watchUntilFor,
} from "@noddle/swarm-ops";
